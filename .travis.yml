language: python
python:
  - '2.7'
install:
  - pip install coveralls
  - pip install pytest-cov
script:
  py.test --cov whatami --doctest-modules whatami
  --pep8 coveralls -v --cov coveralls --cov-report term-missing
after_success:
  coveralls
